import { test, describe, expect } from 'vitest';

describe("Test home page or index page", async ()=>{
  test('my test', () => {
    // ... test without Nuxt environment!
    expect(2+3).toBe(5);
  });
});
